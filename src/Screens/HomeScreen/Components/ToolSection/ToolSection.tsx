import React from 'react';

export type ToolSectionProps = {}

const ToolSection: React.FC<ToolSectionProps> = (props) => {
  return (
    <div>
      ToolSection
    </div>
  );
};

export default ToolSection;
