import publishedPlatforms from './publishedPlatforms';
import allPlatforms from './allPlatforms';
import platform from './platform';

const queries = {
  allPlatforms,
  platform,
  publishedPlatforms,
};

export default queries;
