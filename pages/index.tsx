import React from 'react';
import { HomeScreen } from '@Screens';
import { PageWrapper } from '@Components';

const Home: React.FC = () => (
  <PageWrapper>
    <HomeScreen />
  </PageWrapper>
);

export default Home;
